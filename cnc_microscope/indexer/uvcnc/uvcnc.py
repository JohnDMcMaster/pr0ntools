#!/usr/bin/env python

class Indexer:
	

class Interpreter:
	def __init__(self, stream):
		self.stream = stream

	def run(self):
		for line in self.stream:
			line = line.trim()
			tokens = line.split()
			for token in tokens:
				
		
		
